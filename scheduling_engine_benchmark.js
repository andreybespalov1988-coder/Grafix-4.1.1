'use strict';
const E=require('./scheduling-engine');
function make(n){const db={activeBranchId:1,branches:[{id:1}],scheduleRules:{workStart:'09:00',workEnd:'21:00',slotMinutes:30,maxTeacherDailyMinutes:360,maxGroupDailyMinutes:240,maxConsecutiveMinutes:180,allowedDays:['Понедельник','Вторник','Среда','Четверг','Пятница','Суббота'],softConstraints:{avoidLate:{enabled:true,after:'20:00',priority:'medium'},compact:{enabled:true,priority:'medium'},avoidGaps:{enabled:true,priority:'medium'}}},teachers:[],rooms:[],people:[],lessons:[]}; for(let i=0;i<Math.max(5,Math.ceil(n/5));i++){db.teachers.push({id:i+1,branchId:1,name:'T'+i});db.rooms.push({id:i+1,branchId:1,name:'R'+i,capacity:20})} for(let i=0;i<n;i++){db.people.push({id:i+1,branchId:1,name:'G'+i,group:'G'+i});db.lessons.push({id:i+1,branchId:1,title:'L'+i,group:'G'+i,groupId:i+1,teacher:'T'+(i%db.teachers.length),teacherId:(i%db.teachers.length)+1,room:'R'+(i%db.rooms.length),roomId:(i%db.rooms.length)+1,duration:60,locked:false})} return db}
for(const n of [10,30,50,100]){const db=make(n);const t=Date.now();const r=E.generateVariants(db);const ms=Date.now()-t;console.log(JSON.stringify({lessons:n,ms,variants:r.length,quality:r.map(x=>x.metrics.quality),completion:r.map(x=>x.metrics.completion)}));if(!r.every(x=>x.metrics.hardConflicts===0))process.exitCode=1}
