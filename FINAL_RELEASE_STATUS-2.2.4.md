# Графикс 2.2.4 — validation

Автоматический набор `npm run test:all` после изменений: 10/10 тестовых этапов PASS.

Проверены синтаксис, smoke, storage, restart, A4, UI, release, public/Tilda runtime, print editor runtime, dashboard/teacher runtime.

Windows Electron installer/runtime по-прежнему требует среды с установленными Electron/electron-builder и доступом к npm registry.
