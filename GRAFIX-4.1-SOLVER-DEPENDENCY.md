# GRAFIX 4.1 — Solver Dependency

Dependency:
`or-tools-wasm@0.9.1`

Purpose:
Google OR-Tools CP-SAT compiled to WebAssembly for Node/TypeScript execution.

License:
Apache-2.0.

Current development status:
Declared in `package.json`; not yet installed in the current Linux execution environment because external npm registry access is unavailable.

Windows gate:
The next development run on the real Windows 11 environment must regenerate the npm dependency lock state and execute the real CP-SAT integration suite before packaging is considered.
